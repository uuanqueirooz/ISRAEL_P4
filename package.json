{
  "name": "israel_p4",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npx http-server . -p 8080 -c-1",
    "cy:run": "cypress run --config baseUrl=http://localhost:8080"
  },
  "devDependencies": {
    "cypress": "^12.0.0",
    "http-server": "^14.1.1",
    "wait-on": "^7.0.1"
  }
}